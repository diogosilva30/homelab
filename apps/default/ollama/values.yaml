ollama:
  gpu:
    # -- Disable GPU integration (CPU-only)
    enabled: false

  # -- List of models to pull at container startup
  models:
    pull:
      - mistral:7b
      - llama3.1:8b
      - phi3:mini
